function sum(a, b) {
    const c = a + b;
    return c;
}

function subtract(a, b) {
    return a - b;
}

function multiply(a, b) {
    return a * b;
}

function divide(a, b) {
    return a / b;
}

export default { sum, subtract, multiply, divide };